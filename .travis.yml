os:
  - linux

language: python

install:
  - pip install --upgrade pipenv pip codecov twine
  - pipenv install --dev

jobs:
  include:
    - stage: test
    - stage: packaging
      script:
        - python setup.py sdist bdist_wheel
        - twine check dist/*
    - stage: deploy_test
      if: branch = develop
    - stage: deploy
      if: branch = master
      script:
        - python setup.py sdist bdist_wheel
        - twine upload dist/*
